// переменные
целочисленный массив - для хранения адреса ячеек - locationCells
тип int - количество попаданий - numOfHits = 0

//методы
checkYourself() - принимает ход пользователя в стринге, проверяет, возвращает: Попал,Мимо,Потопил
сеттер setLocationCells() - принимает целочисленный массив, который хранит адрес трех ячеек в виде типа int

//логика
string checkYourself(String userGuess){
    получить ход пользователя в виде строки
    преобразуем стринг в инт
    for(каждая ячейка массива){
        сравнить ход пользователя с местоположением клетки
    }
    если угадал{
        инкрементируем(это увеличение значение переменной) кол-во попаданий
            выясняем, была ли это последняя ячейка
        если кол-вл попаданий = 3{
            возвращаем - потопил
        } если нет {
            значит потопления не произошло - вернуть - попал
        }
    }
    если не угадал {
        пользователь не попал - вернуть - мимо
    }
}